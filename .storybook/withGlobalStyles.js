import '../src/components/index.scss';

// ? Nothing to do... the above "import" is enough
export const withGlobalStyles = () => {
  return { template: '<story />' };
}

export default withGlobalStyles;