import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import PbBadge from './Badge.vue';
import { colors } from '@pb/utils/constants.js';
import { list } from '@pb/Miscellaneous/Icon/list.js';

<Meta
 title="Components/Miscellaneous/PbBadge"
 component={PbBadge}
 argTypes={{
   hasIcon: {
     type: 'boolean',
     defaultValue: false,
   },
    icon: {
      control: { type: 'select', options: [...list()] },
      defaultValue: 'fas fa-times',
    },
   size: {
     control: { type: 'select', options: ['pb-sm', 'pb-md', 'pb-lg'] },
     defaultValue: 'pb-sm',
   },
  title: {
    type: 'string',
    defaultValue: 'Title',
  },
  backgroundColor: {
    control: { type: 'select', options: [ ...colors ] },
    defaultValue: 'primary',
  },
  color: {
    control: { type: 'select', options: [ ...colors ] },
    defaultValue: 'white',
  },
  wrapContent: {
    type: 'boolean',
    defaultValue: false,
  },
 }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { PbBadge },
  template: `
    <PbBadge
      :icon="icon"
      :hasIcon="hasIcon"
      :size="size"
      :title="title"
      :background-color="backgroundColor"
      :color="color"
      :wrap-content="wrapContent"
    />
  `
});

# **PbBadge**
### The `PbBadge` component can be used to highlight an important element in the screen that the user should be aware of

### Purpose and Use case

This component was created to highlight the relevant text for the user on the screen.

## Examples

### Default

<Canvas>
  <Story name="Default">
    {Template.bind({})}
  </Story>
</Canvas>

### Wrap Content

<Canvas>
  <Story name="Wrap Content" args={{ wrapContent: true }}>
    {Template.bind({})}
  </Story>
</Canvas>
