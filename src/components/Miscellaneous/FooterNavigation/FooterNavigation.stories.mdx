import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import PbFooterNavigation from './FooterNavigation.vue';

<Meta
  title="Components/Miscellaneous/PbFooterNavigation"
  component={PbFooterNavigation}
  argTypes={{
    views: {
      type: 'array',
      defaultValue: ['view1', 'view2', 'view3', 'view4']
    },
    finalButtonLabel: {
      type: 'string',
      defaultValue: 'Fechar',
    },
    finalButtonDisabled: {
      type: 'boolean',
      defaultValue: false,
    },
    showFirstBackButton: {
      type: 'boolean',
      defaultValue: false,
    },
    firstBackButtonLabel: {
      type: 'string',
      defaultValue: 'Cancelar',
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { PbFooterNavigation },
  template: `
    <PbFooterNavigation
      :views="views"
      :finalButtonLabel="finalButtonLabel"
      :finalButtonDisabled="finalButtonDisabled"
      :showFirstBackButton="showFirstBackButton"
      :firstBackButtonLabel="firstBackButtonLabel"
    />
  `,
});

# **PbFooterNavigation Component**
### The `PbFooterNavigation` component is used to navigate between pages.

### Purpose and Use case

The footer navigation component receives an array of steps, that is, the screen views.
Having the possibility to change the button label.

## Configuration

### Properties

| Property                    | Type        | Default     | Description                                                                                   |
|:----------------------------|:------------|:------------|:----------------------------------------------------------------------------------------------|
| views                       | Array       |             | List of views                                                                                 |
| finalButtonLabel            | String      | Fechar      | Title of the final button                                                                     |
| finalButtonDisabled         | Boolean     | false       | Disable the final button                                                                      |
| showFirstBackButton         | Boolean     | false       | Shows the first back button, when the component is initialized                                |
| firstBackButtonLabel        | String      | Cancelar    | Title of the first button                                                                     |

### Events

| Event                       | Type        | Emitter                            | Description                                                                                   |
|:----------------------------|:------------|:-----------------------------------|:----------------------------------------------------------------------------------------------|
| changeView                  | String      | Click to next or previous view     | Emits the view to change                                                                      |


## Examples

### Default

<Canvas>
  <Story name="Default" height="auto">
    { Template.bind({}) }
  </Story>
</Canvas>

### With first back button

<Canvas>
  <Story name="WithFirstBackButton" height="auto" args={{ showFirstBackButton: true }}>
    { Template.bind({}) }
  </Story>
</Canvas>
