import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PbSkeletonLoading from './SkeletonLoading.vue';

<Meta
  title="Components/Loadings/PbSkeletonLoading"
  component={PbSkeletonLoading}
  argTypes={{
    mainContainerDimensions: {
      type: 'Array',
      defaultValue: [120, 120],
    },
    secondaryContainerDimensions: {
      type: 'Array',
      defaultValue: [80, 30],
    },
    secondaryDirection: {
      control: {
        type: 'select',
        options: ['row', 'column', 'row-reverse', 'column-reverse'],
      },
      defaultValue: 'row',
    },
    containersAlign: {
      control: {
        type: 'select',
        options: ['flex-start', 'flex-end', 'center', 'space-between', 'space-around', 'space-evenly'],
      },
      defaultValue: 'flex-start',
    },
    mainContainerRadius: {
      type: 'Number',
      defaultValue: 0,
    },
    marginBetweenItems: {
      type: 'Number',
      defaultValue: 0,
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { PbSkeletonLoading },
  template: `
    <div
      style="width: 320px;
      height: 180px;
      display: flex;
      align-items: center;
      justify-content: center;"
    >
      <PbSkeletonLoading
        :main-container-dimensions="mainContainerDimensions"
        :secondary-container-dimensions="secondaryContainerDimensions"
        :secondary-direction="secondaryDirection"
        :containers-align="containersAlign"
        :main-container-radius="mainContainerRadius"
        :marginBetweenItems="marginBetweenItems"
      />
    </div>
  `,
});

# **PbSkeletonLoading Component**

### Description

The PbSkeletonLoading component is a component that shows a skeleton loading.

### Properties

| Property                      | Type                 | Default       | Description
|:------------------------------|:---------------------|:--------------|:-------------------------------
| mainContainerDimensions       | Array                | []            | Dimensions of the main container, [width, height]
| secondaryContainerDimensions  | Array                | []            | Dimensions of the secondary container, [width, height]
| secondaryDirection            | String               | 'row'         | Direction of the secondary container, 'row', 'column', 'row-reverse', 'column-reverse'
| containersAlign               | String               | 'flex-start'  | Alignment of the containers, 'flex-start', 'flex-end', 'center', 'space-between', 'space-around', 'space-evenly'
| mainContainerRadius           | Number               | 0             | Radius of the main container
| marginBetweenItems            | Number               | 0             | Margin between the main and secondary container

## Examples

### Default

<Canvas>
  <Story name="Default">
    {Template.bind({})}
  </Story>
</Canvas>
