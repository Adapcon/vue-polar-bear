import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PbLayoutGrid from './LayoutGrid.vue';

<Meta
  title="Components/Layout/PbLayoutGrid"
  component={PbLayoutGrid}
  argTypes={{
    title: { 
        type: 'string',
        defaultValue: 'Title',
    },
    enableToolBar: { 
        type: 'boolean', 
        defaultValue: false,
    },
    backFunction: {
         type: 'function', 
         defaultValue: () => () => {alert('Hello')},    
    }
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { PbLayoutGrid },
  template: `
    <div style="width: 850px; background-color: white;">
        <PbLayoutGrid 
            title="Page title"
        >
            <template #main>
                <div style="background-color: rgba(173, 172, 250, 0.2); height: 100%;">
                    main
                </div>   
            </template>
            <template #sidebar>
                <div style="background-color: rgba(250, 78, 202, 0.2); height: 560px;">
                    Sidebar
                </div>   
            </template>
            <template #tool-bar>
                <div style="background-color: rgba(155, 239, 88, 0.2); height: 100%;">
                    Tool Bar
                </div>   
            </template>
        </PbLayoutGrid>
    </div>
  `,
});



# **PbLayoutGrid component**
### The `PbLayoutGrid` component used to format the screen with a grid with 3 slots.

### Purpose and Use case

The component's purpose is to organize the screen layout.

## Examples

### Default

<Canvas>
  <Story name="Default" height="auto" parameters={{ layout: 'centered' }}>
    {Template.bind({})}
  </Story>
</Canvas>

