import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import PbLayoutGrid from "./LayoutGrid.vue";

<Meta
  title="Components/Layout/PbLayoutGrid"
  component={PbLayoutGrid}
  argTypes={{
    title: {
      type: "string",
      defaultValue: "Title",
    },
    disableToolBar: {
      type: "boolean",
      defaultValue: false,
    },
    backFunction: {
      action: "backFunction",
    },
    showSideMenu: {
      type: "boolean",
      defaultValue: false,
    },
    newStyle: {
      type: "boolean",
      defaultValue: false,
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { PbLayoutGrid },
  template: `
        <PbLayoutGrid 
            :title="title"
            :disableToolBar="disableToolBar"
            :backFunction="backFunction"
            :showSideMenu="showSideMenu"
            :newStyle="newStyle"
            ref="PbLayoutGrid"
        >
            <template #main>
                <div style="background-color: rgba(173, 172, 250, 0.2);">
                    main
                </div>   
            </template>
            <template #sidebar>
                <div style="background-color: rgba(250, 78, 202, 0.2); height: 100px">
                    Sidebar
                </div>   
            </template>
            <template #tool-bar>
                <div style="background-color: rgba(155, 239, 88, 0.2);">
                    Tool Bar
                </div>   
            </template>
        </PbLayoutGrid>
  `,
});

# **PbLayoutGrid component**

### The `PbLayoutGrid` component used to format the screen with a grid with 3 slots.

### Purpose and Use case

The component's purpose is to organize the screen layout.

## Examples

### Default

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

<!-- TODO: Descobrir como deixar o canvas em mobile -->

<!-- ### Mobile

<Canvas>
  <Story name="Mobile"  parameters={{ layout: 'centered' }} args={{ MobileAuxiliaryButtonMobile: true }}>
    {Template.bind({})}
  </Story>
</Canvas> -->
