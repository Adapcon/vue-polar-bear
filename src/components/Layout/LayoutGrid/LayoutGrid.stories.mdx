import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PbLayoutGrid from './LayoutGrid.vue';

<Meta
  title="Components/Layout/PbLayoutGrid"
  component={PbLayoutGrid}
  argTypes={{
    title: {
        type: 'string',
        defaultValue: 'Title',
    },
    subtitle: {
        type: 'string',
        defaultValue: 'Subtitle',
    },
    headline: {
        type: 'string',
        defaultValue: 'Heading',
    },
    backFunction: {
      action: 'backFunction',
    },
    collapsibleSidebar: {
      type: 'boolean',
      defaultValue: true,
    },
    collapsibleContent: {
      type: 'boolean',
      defaultValue: true,
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { PbLayoutGrid },
  template: `
    <div style="width: 1000px; height: 760px; background-color: white;">
        <PbLayoutGrid
          :title="title"
          :subtitle="subtitle"
          :headline="headline"
          :collapsible-sidebar="collapsibleSidebar"
          :collapsible-content="collapsibleContent"
          :backFunction="backFunction"
          ref="PbLayoutGrid"
        >
          <template #main>
            <div style="background-color: rgba(173, 172, 250, 0.2); height: 100%;">
              Conteúdo principal da página
            </div>
          </template>
          <template #sidebar-tabs>
            <div style="background-color: rgba(250, 175, 78, 0.2);">
              Tabs
            </div>
          </template>
          <template #sidebar-content>
            <div style="background-color: rgba(250, 78, 202, 0.2);">
              Conteúdo da sidebar
            </div>
          </template>
          <template #sidebar-action>
            <div style="background-color: lightblue;">
              Ação extra
            </div>
          </template>
        </PbLayoutGrid>
    </div>
  `,
});



# **PbLayoutGrid component**
### The `PbLayoutGrid` component define the structure of the page.

### Purpose and Use case

The component's purpose is to organize the screen layout in two different parts: sidebar and main.

## Examples

### Default

<Canvas>
  <Story name="Default" height="auto" parameters={{ layout: 'centered' }}>
    {Template.bind({})}
  </Story>
</Canvas>


<!-- TODO: Descobrir como deixar o canvas em mobile -->

<!-- ### Mobile

<Canvas>
  <Story name="Mobile"  parameters={{ layout: 'centered' }} args={{ MobileAuxiliaryButtonMobile: true }}>
    {Template.bind({})}
  </Story>
</Canvas> -->

