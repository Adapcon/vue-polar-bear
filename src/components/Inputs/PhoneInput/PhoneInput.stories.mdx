import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PbPhoneInput from './PhoneInput.vue';
import { colors } from '@pb/utils/constants';

<Meta
  title="Components/Inputs/PbPhoneInput"
  component={PbPhoneInput}
  argTypes={{
    value: {
      type: 'number',
      defaultValue: null,
    },
    required: {
      type: 'boolean',
      defaultValue: false,
    },
    color: {
      control: {
        type: 'select',
        options: [ ...colors ],
      },
      defaultValue: 'primary',
    },
    background: {
      control: {
        type: 'select',
        options: [ ...colors ],
      },
      defaultValue: 'transparent',
    },
    inputStyle: {
      control: {
        type: 'select',
        options: ['regular', 'outline'],
      },
      defaultValue: 'outline',
    },
    disabled: {
      type: 'boolean',
      defaultValue: false,
    },
  }}
/>

export const Template = (args, { argTypes}) => ({
  props: Object.keys(argTypes),
  components: { PbPhoneInput },
  template: `
    <PbPhoneInput
      v-model="value"
      :required="required"
      :color="color"
      :background="background"
      :inputStyle="inputStyle"
      :disabled="disabled"
    />
  `,
});

# **PbPhone**
### The `PbPhone` component is used as input for phones.

### Purpose and Use Case
Its main purpose is to group input and validation together with a component.

## Examples

### Default

<Canvas>
  <Story name="Default">
    {Template.bind({})}
  </Story>
</Canvas>
