import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PbTextInput from './TextInput.vue';
import { colors } from '@pb/utils/constants.js';

<Meta
  title="Components/Inputs/PbTextInput"
  component={ PbTextInput }
  argTypes={{
    value: {
      type: 'string',
      defaultValue: '',
    },
    placeholder: {
      type: 'string',
      defaultValue: 'Placeholder',
    },
    disabled: {
      type: 'boolean',
      defaultValue: false,
    },
    color: {
      control: { type: 'select', options: [ ...colors ] },
      defaultValue: 'gray-20',
    },
    borderColor: {
      control: { type: 'select', options: [ ...colors ] },
      defaultValue: 'gray-20',
    },
    background: {
      control: { type: 'select', options: [ ...colors ] },
      defaultValue: 'transparent',
    },
    validator: {
      type: 'function',
      defaultValue: null,
    },
    focus: {
      type: 'boolean',
      defaultValue: false,
    },
    maxLength: {
      type: 'number',
      defaultValue: null,
    },
  }}
/>

# **PbTextInput component**
### The `PbTextInput` was created to capture texts through user input.

### Purpose and Use case

The `PbTextInput` component is used to capture text input from the user.
It uses v-model, similar to a regular input.

The component has a `validator` property similar to the vue properties validator.
This property must contain all validations, if it is valid, return `true`
and if validation fails, it should return `false`.
This will cause all changes to the `value` to be validated and if an error
occurs visually modify the component.

## Examples

### Default

export const Template = (args, { argTypes}) => ({
  props: Object.keys(argTypes),
  components: { PbTextInput },
  template: `
    <PbTextInput
      v-model="value"
      :placeholder="placeholder"
      :disabled="disabled"
      :border-color="borderColor"
      :validator="validator"
      :background="background"
      :color="color"
      :focus="focus"
      :max-length="maxLength"
    />
  `,
})

### Default
<Canvas>
  <Story name="Default" height="auto">
    {Template.bind({})}
  </Story>
</Canvas>

### Input Error
<Canvas>
  <Story
    name="Error"
    height="auto"
    args={{
      value: "Error",
      validator: () => false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Focused Input
<Canvas>
  <Story
    name="Focused"
    height="auto"
    args={{
      focus: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
