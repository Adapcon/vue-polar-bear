import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PbTaxIdInput from './TaxIdInput.vue';
import { colors } from '@pb/utils/constants';
import { documentTypes } from './documentTypes.js';

<Meta
  title="Components/Inputs/PbTaxIdInput"
  component={PbTaxIdInput}
  argTypes={{
    value: {
      type: 'number',
      defaultValue: null,
    },
    allowedDocuments: {
      control: {
        type: 'inline-check',
        options: [ ...documentTypes ],
      },
      defaultValue: [ 'cnpj' ],
    },
    placeholder: {
      type: 'string',
      defaultValue: 'Insira o número do documento!',
    },
    required: {
      type: 'boolean',
      defaultValue: false,
    },
    background: {
      control: {
        type: 'select',
        options: [ ...colors ],
      },
      defaultValue: 'transparent',
    },
    color: {
      control: {
        type: 'select',
        options: [ ...colors ],
      },
      defaultValue: 'primary',
    },
  }}
/>


export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { PbTaxIdInput },
  template: `
    <PbTaxIdInput
      v-model="value"
      :background="background"
      :color="color"
      :placeholder="placeholder"
      :allowed-documents="allowedDocuments"
      :required="required"
    />
  `,
});


# **PbTaxIdInput**
### The `PbTaxIdInput` component is used as input for get official company register document.

### Purpose and Use Case
Its main purpose is to group input and validation together with a component.

### Usage
With `allowedDocuments`, you can select the type of document you want to use as input, and the placeholder will be automatically set, unless you want to use a custom placeholder.
You can also set two different input types, to support more documents.

When the `allowedDocuments`, has more than one type, the placeholder will be empty, unless you set a custom placeholder.

If you want to use a custom placeholder, you can set it using the `placeholder` prop.

If the input is required, you can set it using the `required` prop.

The `background` and `color` props are used to set the background and color of the input.

## Examples

### Default

<Canvas>
  <Story name="Default">
    {Template.bind({})}
  </Story>
</Canvas>

### With CPF and CNPJ

<Canvas>
  <Story name="Only show form"
    args={{
      allowedDocuments: [ 'cpf', 'cnpj' ],
      placeholder: 'CPF or CNPJ',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
