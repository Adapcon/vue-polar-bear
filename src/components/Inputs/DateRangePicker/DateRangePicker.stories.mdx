import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PbDateRangePicker from './DateRangePicker.vue';
import { colors } from '@pb/utils/constants.js';

<Meta
  title="Components/Inputs/PbDateRangePicker"
  component={ PbDateRangePicker }
  argTypes={{
    value: {
      type: 'Object',
      defaultValue: { startDate: '', endDate: '' },
    },
    calendariesPosition: {
      type: 'String',
      defaultValue: 'right',
      control: {
        type: 'select',
        options: ['right', 'left'],
      },
    },
    inputStyle: {
      type: 'String',
      defaultValue: 'background-light',
      control: {
        type: 'select',
        options: ['background-light', 'outline'],
      },
    }
  }}
/>

export const Template = (args, { argTypes}) => ({
  props: Object.keys(argTypes),
  components: { PbDateRangePicker },
  template: `
  <div style="width: 100%; height: 480px; display: flex; justify-content: center">
    <PbDateRangePicker
      style="width: 265px;"
      :value="value"
      :calendaries-position="calendariesPosition"
      :input-style="inputStyle"
    />
  </div>
  `,
});

# **PbDateRangePicker component**
### The `PbDateRangePicker` was created to capture a date range through user input.

### Purpose and Use case

The `PbDateRangePicker` component is used to capture a date range input from the user.

This component have some validations that don´t allow the user to select a date range that is not valid.
For example, if the user select a start date that is after the end date, the component will not allow the user to select the end date, and vice versa.

### Usage

```html
<PbDateRangePicker
  :value="{ startDate: '', endDate: '' }"
  :calendaries-position="right | left"
  :input-style="background-light | outline"
/>
```

## Props

This component have the following props:

| Prop | Type | Default      | Description |
| ---  | ---  | ------------ | --- |
| value | Object | { startDate: '', endDate: '' } | Object used to set the value of the input, always have to pass the startDate and endDate as the usage example |
| calendaries-position | String | right | Used to set the side that the calendary opens |
| input-style | String | background-light | Used to set the style of the input |


### Default

<Canvas>
  <Story name="Default" height="auto">
    {Template.bind({})}
  </Story>
</Canvas>
